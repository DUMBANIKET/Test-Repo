name: Hello World

on:
  push:
    branches:
      - main
    paths:
      - 'test.csv'

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: echo "Hello, World!"
